---
platform: linux

image: docker:///pcfservicesenablement/services-enablement-ci

inputs:
  - name: service-metrics-release

run:
  path: service-metrics-release/ci/scripts/system-tests.sh

params:
  CF_API:
  CF_USERNAME:
  CF_PASSWORD:
  DOPPLER_ADDR:
  DEPLOYMENT_NAME:
